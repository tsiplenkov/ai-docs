---
description: Switch to specific DevOps context (cluster-charts, shared-data, apps)
argument-hint: <context-name>
---

# Load Specific Context

Load documentation for context: **$ARGUMENTS**

Use `mcp__library-mcp__get_by_tag` with tag="$ARGUMENTS" and limit=1 to load the configuration documentation.

Show a brief summary of the loaded context and confirm readiness.

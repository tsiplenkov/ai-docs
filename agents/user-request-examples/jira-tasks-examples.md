# Jira Tasks - Примеры запросов пользователей

Практические примеры работы с субагентом **jira-tasks** для управления задачами в Jira через CLI.

## 📍 Контекст работы

```bash
# Агент работает в любой директории
# Для явного вызова используйте:
claude
> Use jira-tasks subagent to show my current tasks
```

## ✨ Особенности агента

**Работа с Jira через jira-cli:**
- ✅ Read-only операции без подтверждения
- ⚠️ Создание и изменение требуют подтверждения
- 📊 Поддержка JQL для сложных запросов
- 🏷️ Работа с метками, приоритетами, спринтами
- ⏱️ Логирование времени работы

---

## 📋 Просмотр списка задач

### Пример 1: Мои задачи в работе

**Запрос:**
```
Покажи мои задачи в работе
```

**Что делает агент:**
```bash
# Выполняет команду
jira issue list -a$(jira me) -s"In Progress"
```

**Результат:**
```
🔄 Задачи в работе (3):

TASK-123: Реализация API для загрузки файлов
  Приоритет: High
  Создана: 2025-11-18

TASK-456: Рефакторинг модуля аутентификации
  Приоритет: Medium
  Создана: 2025-11-19

BUG-789: Исправление утечки памяти
  Приоритет: High
  Создана: 2025-11-20
```

---

### Пример 2: Задачи за последнюю неделю

**Запрос:**
```
Покажи все задачи за последнюю неделю
```

**Что делает агент:**
```bash
jira issue list --created -7d --order-by created --reverse
```

**Результат:**
Список всех задач, созданных за последние 7 дней, отсортированный по дате создания.

---

### Пример 3: Критичные задачи

**Запрос:**
```
Найди все задачи с высоким приоритетом в статусе To Do
```

**Что делает агент:**
```bash
jira issue list -yHigh -s"To Do"
```

**Результат:**
```
⚠️ Критичные задачи (2):

TASK-555: Оптимизация базы данных
  Метки: backend, performance
  Создана: 2025-11-19

BUG-666: Ошибка 500 при авторизации
  Метки: backend, security
  Создана: 2025-11-20
```

---

### Пример 4: Фильтрация по меткам

**Запрос:**
```
Покажи все задачи с меткой backend в статусе In Progress
```

**Что делает агент:**
```bash
jira issue list -lbackend -s"In Progress"
```

---

### Пример 5: Сложный JQL запрос

**Запрос:**
```
Найди все баги в проекте MYPROJ, назначенные на меня, которые не закрыты
```

**Что делает агент:**
```bash
jira issue list -q "project = MYPROJ AND type = Bug AND assignee = currentUser() AND status != Done"
```

---

## 🔍 Просмотр деталей задачи

### Пример 6: Детали конкретной задачи

**Запрос:**
```
Покажи детали задачи TASK-123
```

**Что делает агент:**
```bash
jira issue view TASK-123
```

**Результат:**
Полная информация о задаче: заголовок, описание, статус, приоритет, assignee, метки, даты и т.д.

---

### Пример 7: Задача с комментариями

**Запрос:**
```
Покажи TASK-123 со всеми комментариями
```

**Что делает агент:**
```bash
jira issue view TASK-123 --comments 50
```

---

## ➕ Создание новых задач

### Пример 8: Простой баг

**Запрос:**
```
Создай баг: "API возвращает 500 при запросе к /users"
```

**Что делает агент:**
```
⚠️ Требуется подтверждение для создания задачи:

Тип: Bug
Заголовок: API возвращает 500 при запросе к /users
Приоритет: Medium (по умолчанию)

Подтвердить? [y/n]
```

**После подтверждения:**
```bash
jira issue create \
  -tBug \
  -s"API возвращает 500 при запросе к /users" \
  --no-input
```

**Результат:**
```
✅ Создан баг BUG-1001
```

---

### Пример 9: Задача с полными параметрами

**Запрос:**
```
Создай задачу "Реализовать экспорт данных в CSV" с приоритетом High, метками backend и feature, описание: "Пользователи должны иметь возможность экспортировать данные в CSV формат"
```

**Что делает агент:**
```
⚠️ Требуется подтверждение для создания задачи:

Тип: Task
Заголовок: Реализовать экспорт данных в CSV
Описание: Пользователи должны иметь возможность экспортировать данные в CSV формат
Приоритет: High
Метки: backend, feature

Подтвердить? [y/n]
```

**После подтверждения:**
```bash
jira issue create \
  -tTask \
  -s"Реализовать экспорт данных в CSV" \
  -b"Пользователи должны иметь возможность экспортировать данные в CSV формат" \
  -yHigh \
  -lbackend,feature \
  --no-input
```

---

### Пример 10: User Story с эпиком

**Запрос:**
```
Создай story "Пользователь может изменить аватар" в эпике EPIC-50
```

**Что делает агент:**
```bash
# После подтверждения
jira issue create \
  -tStory \
  -s"Пользователь может изменить аватар" \
  -P EPIC-50 \
  --no-input
```

---

## 🔄 Изменение статуса задач

### Пример 11: Простое изменение статуса

**Запрос:**
```
Переведи TASK-123 в In Progress
```

**Что делает агент:**
```
⚠️ Требуется подтверждение:

Задача: TASK-123
Новый статус: In Progress

Подтвердить? [y/n]
```

**После подтверждения:**
```bash
jira issue move TASK-123 "In Progress"
```

**Результат:**
```
✅ TASK-123 переведена в статус "In Progress"
```

---

### Пример 12: Изменение статуса с комментарием

**Запрос:**
```
Переведи TASK-456 в Done с комментарием "Реализовано и протестировано"
```

**Что делает агент:**
```bash
# После подтверждения
jira issue move TASK-456 "Done" --comment "Реализовано и протестировано"
```

---

### Пример 13: Закрытие с резолюцией

**Запрос:**
```
Закрой BUG-789 с резолюцией Fixed
```

**Что делает агент:**
```bash
# После подтверждения
jira issue move BUG-789 "Done" -RFixed
```

---

## ⏱️ Логирование времени

### Пример 14: Добавить время работы

**Запрос:**
```
Добавь 3 часа работы к TASK-123
```

**Что делает агент:**
```
⚠️ Требуется подтверждение для логирования времени:

Задача: TASK-123
Время: 3 часа

Подтвердить? [y/n]
```

**После подтверждения:**
```bash
jira issue worklog add TASK-123

# Агент инструктирует:
"В интерактивном режиме введите:
- Время: 3h
- Дата: нажмите Enter для текущей даты
- Комментарий: опционально"
```

---

### Пример 15: Логирование за вчера

**Запрос:**
```
Залогируй 4 часа на TASK-456 за вчера с комментарием "Работа над API"
```

**Что делает агент:**
```bash
# После подтверждения запускает интерактивный режим
jira issue worklog add TASK-456

# Инструкции:
# Time spent: 4h
# Started at: 2025-11-20  (вчерашняя дата)
# Comment: Работа над API
```

---

## 💬 Комментарии

### Пример 16: Добавить комментарий

**Запрос:**
```
Добавь комментарий к TASK-123: "Реализовал 70%, осталось добавить unit тесты"
```

**Что делает агент:**
```bash
# После подтверждения
jira issue comment add TASK-123 "Реализовал 70%, осталось добавить unit тесты"
```

**Результат:**
```
✅ Комментарий добавлен к TASK-123
```

---

### Пример 17: Многострочный комментарий

**Запрос:**
```
Добавь комментарий к BUG-789:
"Проблема воспроизведена.
Root cause: неправильная обработка null значений.
План: добавить проверки и тесты."
```

**Что делает агент:**
```bash
# После подтверждения
jira issue comment add BUG-789 "Проблема воспроизведена.
Root cause: неправильная обработка null значений.
План: добавить проверки и тесты."
```

---

## ✏️ Редактирование задач

### Пример 18: Изменить заголовок

**Запрос:**
```
Измени заголовок TASK-555 на "Оптимизация PostgreSQL индексов"
```

**Что делает агент:**
```bash
# После подтверждения
jira issue edit TASK-555 -s"Оптимизация PostgreSQL индексов" --no-input
```

---

### Пример 19: Изменить приоритет

**Запрос:**
```
Повысь приоритет TASK-666 до High
```

**Что делает агент:**
```bash
# После подтверждения
jira issue edit TASK-666 -yHigh --no-input
```

---

### Пример 20: Добавить метки

**Запрос:**
```
Добавь метки urgent и security к BUG-777
```

**Что делает агент:**
```bash
# После подтверждения
jira issue edit BUG-777 -lurgent,security --no-input
```

---

### Пример 21: Комплексное редактирование

**Запрос:**
```
Обнови TASK-888: заголовок "Миграция на PostgreSQL 15", приоритет High, метки database,migration
```

**Что делает агент:**
```bash
# После подтверждения
jira issue edit TASK-888 \
  -s"Миграция на PostgreSQL 15" \
  -yHigh \
  -ldatabase,migration \
  --no-input
```

---

## 📊 Отчеты и аналитика

### Пример 22: Ежедневный статус

**Запрос:**
```
Покажи мой ежедневный статус: что завершил вчера, что в работе, что в планах
```

**Что делает агент:**
```bash
# Завершено вчера
jira issue list -a$(jira me) -s"Done" --created -2d

# В работе
jira issue list -a$(jira me) -s"In Progress"

# В очереди (топ-5)
jira issue list -a$(jira me) -s"To Do" --order-by priority --reverse
```

**Результат:**
```
📊 Ежедневный статус отчет

✅ Завершено вчера (2):
- TASK-123: Реализация API
- BUG-456: Исправление бага авторизации

🔄 В работе (2):
- TASK-789: Рефакторинг кода
- TASK-555: Оптимизация базы данных

📋 В очереди (приоритетные):
- TASK-111: [High] Настройка CI/CD
- TASK-222: [Medium] Обновление документации
- TASK-333: [Medium] Code review
```

---

### Пример 23: Недельный отчет

**Запрос:**
```
Покажи статистику по задачам за неделю
```

**Что делает агент:**
```bash
# Созданные за неделю
jira issue list --created -7d

# Завершенные за неделю
jira issue list -a$(jira me) -s"Done" --created -7d

# Анализирует и предоставляет статистику
```

**Результат:**
```
📈 Статистика за неделю:

📝 Создано: 12 задач
✅ Завершено: 8 задач
🔄 В работе: 3 задачи
📋 В очереди: 5 задач

Типы:
- Tasks: 7
- Bugs: 4
- Stories: 1

Приоритеты:
- High: 3
- Medium: 6
- Low: 3
```

---

### Пример 24: Просроченные задачи

**Запрос:**
```
Покажи мои просроченные задачи
```

**Что делает агент:**
```bash
jira issue list -a$(jira me) -q "duedate < now() AND status != Done"
```

---

## 🏃 Спринты

### Пример 25: Задачи текущего спринта

**Запрос:**
```
Покажи мои задачи в текущем спринте
```

**Что делает агент:**
```bash
jira sprint list --current
```

---

### Пример 26: Добавить задачу в спринт

**Запрос:**
```
Добавь TASK-999 в спринт 42
```

**Что делает агент:**
```bash
# После подтверждения
jira issue move TASK-999 --sprint 42
```

---

## 🔗 Связи между задачами

### Пример 27: Привязать задачи

**Запрос:**
```
Свяжи TASK-111 с TASK-222
```

**Что делает агент:**
```bash
# После подтверждения
jira issue link TASK-111 TASK-222
```

---

### Пример 28: Клонировать задачу

**Запрос:**
```
Клонируй задачу BUG-888 для другого окружения
```

**Что делает агент:**
```bash
# После подтверждения
jira issue clone BUG-888
```

---

## 🎯 Комплексные сценарии

### Пример 29: Полный цикл работы над задачей

**Запрос:**
```
Начинаю работу над TASK-500:
1. Переведи в In Progress
2. Добавь комментарий "Начал работу"
3. Залогируй 2 часа работы
```

**Что делает агент:**
```
⚠️ Требуется подтверждение для серии операций:

1. Перевод TASK-500 в "In Progress"
2. Добавление комментария "Начал работу"
3. Логирование 2 часов работы

Подтвердить? [y/n]
```

**После подтверждения:**
```bash
# Шаг 1
jira issue move TASK-500 "In Progress"

# Шаг 2
jira issue comment add TASK-500 "Начал работу"

# Шаг 3
jira issue worklog add TASK-500
# (интерактивный ввод: 2h)
```

---

### Пример 30: Завершение задачи

**Запрос:**
```
Завершаю TASK-500:
1. Добавь комментарий "Реализовано, протестировано, готово к review"
2. Залогируй 3 часа
3. Переведи в Code Review
```

**Что делает агент:**
```bash
# После подтверждения всех шагов

# Шаг 1
jira issue comment add TASK-500 "Реализовано, протестировано, готово к review"

# Шаг 2
jira issue worklog add TASK-500
# (3h)

# Шаг 3
jira issue move TASK-500 "Code Review"
```

---

### Пример 31: Создание бага из production инцидента

**Запрос:**
```
Production инцидент: создай критичный баг:
Заголовок: "API /users/profile возвращает 500"
Описание: "После деплоя версии 2.5.0 эндпоинт /users/profile возвращает 500 Internal Server Error для 30% запросов"
Приоритет: Highest
Метки: production, critical, api
```

**Что делает агент:**
```bash
# После подтверждения
jira issue create \
  -tBug \
  -s"API /users/profile возвращает 500" \
  -b"После деплоя версии 2.5.0 эндпоинт /users/profile возвращает 500 Internal Server Error для 30% запросов" \
  -yHighest \
  -lproduction,critical,api \
  --no-input
```

**Результат:**
```
✅ Создан критичный баг BUG-1111

Следующие шаги:
1. Назначить на on-call инженера
2. Создать incident channel
3. Начать расследование
```

---

### Пример 32: Batch операции

**Запрос:**
```
Найди все баги с меткой backend в статусе To Do и покажи их приоритеты
```

**Что делает агент:**
```bash
jira issue list -lbackend -tBug -s"To Do"
```

**Анализирует результаты и предоставляет:**
```
🐛 Backend баги в To Do (5):

Highest:
- BUG-1111: API /users/profile возвращает 500

High:
- BUG-999: Memory leak в auth service
- BUG-888: Неправильная обработка null

Medium:
- BUG-777: Typo в error message
- BUG-666: Missing validation

Рекомендация: Начните с BUG-1111 (Highest priority)
```

---

## 🔍 Расширенные JQL запросы

### Пример 33: Комплексный поиск

**Запрос:**
```
Найди все задачи:
- Проект MYPROJ
- Созданные за последний месяц
- Не назначенные
- Статус To Do или In Progress
```

**Что делает агент:**
```bash
jira issue list -q "project = MYPROJ AND created >= -30d AND assignee is EMPTY AND status in ('To Do', 'In Progress')"
```

---

### Пример 34: Поиск по тексту

**Запрос:**
```
Найди все задачи со словом "authentication" в заголовке или описании
```

**Что делает агент:**
```bash
jira issue list -q "text ~ 'authentication'"
```

---

## 💡 Автоматизация и скрипты

### Пример 35: Утренний чеклист

**Запрос:**
```
Выполни утренний чеклист:
1. Покажи просроченные задачи
2. Задачи в работе
3. Топ-5 приоритетных в очереди
```

**Что делает агент:**
```bash
# 1. Просроченные
jira issue list -a$(jira me) -q "duedate < now() AND status != Done"

# 2. В работе
jira issue list -a$(jira me) -s"In Progress"

# 3. Топ-5 в очереди
jira issue list -a$(jira me) -s"To Do" --order-by priority --reverse | head -5
```

**Результат:**
```
🌅 Утренний чеклист:

⚠️ Просроченные (1):
- TASK-444: [Due: 2025-11-19] Обновить документацию

🔄 В работе (2):
- TASK-555: Оптимизация базы данных
- BUG-666: Исправление memory leak

📋 Приоритетные в очереди (5):
1. TASK-111: [High] Настройка CI/CD
2. TASK-222: [High] Security аудит
3. TASK-333: [Medium] Code review
4. TASK-444: [Medium] Рефакторинг
5. TASK-555: [Medium] Документация API
```

---

### Пример 36: Еженедельный обзор

**Запрос:**
```
Сделай недельный обзор моей работы
```

**Что делает агент:**
```bash
# Завершенные за неделю
jira issue list -a$(jira me) -s"Done" --created -7d

# Созданные мной за неделю
jira issue list --reporter $(jira me) --created -7d

# Потраченное время (если доступно)
# Анализ и статистика
```

**Результат:**
```
📊 Недельный обзор (21-28 ноября):

✅ Завершено: 12 задач
  - Tasks: 7
  - Bugs: 4
  - Stories: 1

📝 Создано: 5 задач
  - Tasks: 3
  - Bugs: 2

⏱️ Залогировано времени: 32h

🏆 Highlights:
- Реализована новая API v2
- Исправлены 4 критичных бага
- Code review: 15 PR

🎯 На следующую неделю:
- 8 задач в очереди
- 2 в работе
```

---

## 📚 Best Practices

### Пример 37: Правильное именование задач

**❌ Плохо:**
```
Создай задачу "fix bug"
Создай задачу "update"
Создай задачу "new feature"
```

**✅ Хорошо:**
```
Создай баг "[API] Fix 500 error on /users endpoint when email is null"
Создай задачу "[Frontend] Update user profile layout to Material Design 3"
Создай story "[Backend] Add support for batch file uploads via S3"
```

---

### Пример 38: Структурированное описание

**Запрос (правильный):**
```
Создай баг с заголовком "[API] Memory leak in WebSocket connections"
Описание:
"## Проблема
При длительной работе WebSocket соединений наблюдается рост потребления памяти.

## Воспроизведение
1. Открыть 10+ WebSocket соединений
2. Оставить на 1+ час
3. Наблюдать рост памяти через Grafana

## Ожидаемое поведение
Память должна быть стабильной

## Окружение
- Production cluster
- Service version: 2.5.0
- Node.js: 18.17.0

## Дополнительно
Grafana: https://grafana.example.com/d/...
Связанные задачи: TASK-777"

Приоритет: High
Метки: backend, websocket, memory-leak, production
```

---

## 🚨 Troubleshooting

### Пример 39: Проблема с аутентификацией

**Запрос:**
```
Команды jira не работают, ошибка аутентификации
```

**Что делает агент:**
```bash
# Проверяет конфигурацию
cat ~/.config/jira/.config.yml

# Предлагает решение:
"Попробуйте переинициализировать конфигурацию:
jira init

Или проверьте:
1. API токен в ~/.config/jira/.config.yml
2. Правильность URL Jira instance
3. Доступ к сети"
```

---

### Пример 40: Неизвестный статус

**Запрос:**
```
Не могу перевести задачу в статус "QA Testing"
```

**Что делает агент:**
```
Статусы зависят от workflow проекта.

Проверьте доступные статусы:
1. Откройте задачу в браузере
2. Посмотрите доступные переходы
3. Используйте точное название статуса

Возможно, правильное название:
- "QA Testing"
- "Testing"
- "QA Review"

Для просмотра текущего статуса:
jira issue view TASK-123
```

---

## 🎯 Сценарии работы команды

### Пример 41: Planning meeting

**Запрос:**
```
Подготовь данные для planning meeting:
- Все задачи из backlog
- Нераспределенные задачи
- Задачи без оценки
```

**Что делает агент:**
```bash
# Backlog
jira issue list -s"To Do" --order-by priority --reverse

# Нераспределенные
jira issue list -q "assignee is EMPTY AND status = 'To Do'"

# Без оценки (если используется поле)
jira issue list -q "status = 'To Do' AND 'Story Points' is EMPTY"
```

---

### Пример 42: Release planning

**Запрос:**
```
Покажи все задачи для версии 2.0.0
```

**Что делает агент:**
```bash
jira issue list -q "fixVersion = '2.0.0'"
```

**Анализирует:**
```
📦 Release 2.0.0 (15 задач):

✅ Done: 10
🔄 In Progress: 3
📋 To Do: 2

По типам:
- Features: 8
- Bugs: 5
- Tasks: 2

🎯 Готовность: 67%

⚠️ Блокеры:
- TASK-999: Waiting for API documentation
```

---

## 📖 Дополнительные ресурсы

- [jira-cli GitHub](https://github.com/ankitpokhrel/jira-cli)
- [Документация jira-tasks агента](../jira-tasks.md)
- [JQL Reference](https://support.atlassian.com/jira-service-management-cloud/docs/use-advanced-search-with-jira-query-language-jql/)

---

## ⚠️ Важные напоминания

### Разрешения
- ✅ Просмотр задач - без подтверждения
- ⚠️ Создание/изменение - требует подтверждения
- ⚠️ Удаление - требует подтверждения

### Статусы
Используйте точные названия статусов из вашего workflow:
- Регистр важен: "In Progress" ≠ "in progress"
- Пробелы важны: "In Progress" ≠ "InProgress"
- Проверяйте через `jira issue view EXISTING-TASK`

### JQL
Для сложных запросов используйте JQL:
- Поддерживает AND, OR, NOT
- Множество функций: currentUser(), now(), startOfWeek()
- Работа с custom fields

### Логирование времени
- Формат: 1h, 30m, 2h 30m, 1d, 1w
- Можно указывать дату начала
- Добавляйте комментарии для контекста

---

**Версия:** 1.0
**Дата:** 2025-11-21
**Автор:** DevOps Team

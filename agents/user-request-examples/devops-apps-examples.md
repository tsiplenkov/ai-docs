# DevOps Apps - Примеры запросов пользователей

Практические примеры работы с субагентом **devops-apps** для управления приложениями в Kubernetes.

## 📍 Контекст работы

```bash
cd ~/projects/<project>/apps/<app-project>/<app-service>/
claude
# Субагент devops-apps активируется автоматически
```

---

## 🔢 Масштабирование

### Пример 1: Изменить количество реплик

**Запрос:**
```
Увеличь количество реплик до 5
```

**Что делает агент:**
1. ✅ Читает `kuber/values.yaml`
2. ✅ Показывает текущее значение: `replicaCount: 2`
3. ✅ Проверяет deployment: `kubectl get deployment <app> -n <namespace>`
4. ⚠️ Запрашивает подтверждение на изменение
5. ✅ Обновляет `replicaCount: 5`
6. ✅ Проверяет наличие CI/CD и предлагает варианты применения

### Пример 2: Масштабирование + ресурсы

**Запрос:**
```
Увеличь количество реплик до 5 и добавь memory limit 2Gi
```

**Что делает агент:**
1. ✅ Читает текущие значения
2. ✅ Показывает изменения:
   - `replicaCount: 2 → 5`
   - `resources.limits.memory: 1Gi → 2Gi`
3. ⚠️ Запрашивает подтверждение
4. ✅ Обновляет values.yaml
5. ✅ Показывает варианты применения (CI/CD или helmfile sync)

---

## 💾 Ресурсы (CPU/Memory)

### Пример 1: Настроить ресурсы

**Запрос:**
```
Настрой ресурсы: memory limit 2Gi, memory request 1Gi, cpu limit 1000m, cpu request 500m
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ✅ Проверяет текущее использование: `kubectl top pod -n <namespace>`
3. ⚠️ Запрашивает подтверждение
4. ✅ Обновляет:
   ```yaml
   resources:
     requests:
       cpu: 500m
       memory: 1Gi
     limits:
       cpu: 1000m
       memory: 2Gi
   ```
5. ✅ После применения проверяет: `kubectl top pod`

### Пример 2: Увеличить только memory

**Запрос:**
```
Увеличь memory limit до 4Gi
```

---

## 🌐 Ingress и TLS

### Пример 1: Настроить Ingress с TLS

**Запрос:**
```
Настрой Ingress для api.example.com с автоматическим TLS через cert-manager
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ⚠️ Запрашивает подтверждение
3. ✅ Добавляет:
   ```yaml
   ingress:
     enabled: true
     className: nginx
     annotations:
       cert-manager.io/cluster-issuer: letsencrypt-prod
     hosts:
       - host: api.example.com
         paths:
           - path: /
             pathType: Prefix
     tls:
       - secretName: api-example-com-tls
         hosts:
           - api.example.com
   ```
4. ✅ После применения проверяет:
   - `kubectl get ingress -n <namespace>`
   - `kubectl get certificate -n <namespace>`
   - `kubectl describe certificate api-example-com-tls -n <namespace>`

### Пример 2: Добавить дополнительный path

**Запрос:**
```
Добавь в Ingress path /api с pathType Prefix
```

---

## 🔐 Переменные окружения и секреты

### Пример 1: Добавить простую переменную

**Запрос:**
```
Добавь переменную окружения LOG_LEVEL со значением debug
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ⚠️ Запрашивает подтверждение
3. ✅ Добавляет:
   ```yaml
   env:
     - name: LOG_LEVEL
       value: "debug"
   ```

### Пример 2: Добавить переменные из ExternalSecret

**Запрос:**
```
Добавь переменные окружения API_KEY и DB_PASSWORD из ExternalSecret app-secrets
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ✅ Определяет namespace из конфигурации
3. ⚠️ Запрашивает подтверждение
4. ✅ Настраивает ExternalSecret (если нет):
   ```yaml
   externalSecrets:
     enabled: true
     secrets:
       - name: app-secrets
         data:
           - secretKey: API_KEY
             vault_path: "{{ .Values.dynamic.project }}/{{ .Values.dynamic.ns }}/api-service"
             vaultKey: api_key
           - secretKey: DB_PASSWORD
             vault_path: "{{ .Values.dynamic.project }}/{{ .Values.dynamic.ns }}/api-service"
             vaultKey: db_password
   ```
5. ✅ Добавляет использование:
   ```yaml
   env:
     - name: API_KEY
       valueFrom:
         secretKeyRef:
           name: app-secrets
           key: API_KEY
     - name: DB_PASSWORD
       valueFrom:
         secretKeyRef:
           name: app-secrets
           key: DB_PASSWORD
   ```
6. ✅ Проверяет создание: `kubectl get externalsecret/secret -n <namespace>`

### Пример 3: Создать секрет в Vault и настроить ExternalSecret

**Запрос:**
```
Создай в Vault секрет для API_KEY и настрой ExternalSecret
```

**Что делает агент:**
1. ✅ Определяет путь в Vault: `<project>/<namespace>/<app-name>`
2. ✅ Проверяет существование: `vault kv get -mount=secrets <path>`
3. ⚠️ Если нет - создает: `vault kv put -mount=secrets <path> api_key=<value>`
4. ⚠️ Настраивает ExternalSecret в values.yaml
5. ✅ Применяет и проверяет

---

## 📈 Автомасштабирование (HPA)

### Пример 1: Настроить HPA

**Запрос:**
```
Настрой автоматическое масштабирование от 2 до 10 реплик при CPU > 70%
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ⚠️ Запрашивает подтверждение
3. ✅ Добавляет:
   ```yaml
   autoscaling:
     enabled: true
     minReplicas: 2
     maxReplicas: 10
     targetCPUUtilizationPercentage: 70
   ```
4. ✅ После применения проверяет:
   - `kubectl get hpa -n <namespace>`
   - `kubectl describe hpa <app> -n <namespace>`
   - `kubectl top pod -n <namespace>`

### Пример 2: Добавить memory в HPA

**Запрос:**
```
Добавь в HPA метрику по памяти 80%
```

---

## 🩺 Health Checks

### Пример 1: Настроить probes

**Запрос:**
```
Настрой liveness probe на /health и readiness probe на /ready
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ⚠️ Запрашивает подтверждение
3. ✅ Добавляет:
   ```yaml
   livenessProbe:
     httpGet:
       path: /health
       port: 8080
     initialDelaySeconds: 30
     periodSeconds: 10
   readinessProbe:
     httpGet:
       path: /ready
       port: 8080
     initialDelaySeconds: 5
     periodSeconds: 5
   ```

---

## 🐛 Дебаг и диагностика

### Пример 1: Почему приложение не стартует?

**Запрос:**
```
Почему приложение api-service не стартует?
```

**Что делает агент (всё без подтверждения):**
1. ✅ `kubectl get pods -n <namespace> -l app.kubernetes.io/name=api-service`
2. ✅ `kubectl describe pod <pod-name> -n <namespace>`
3. ✅ `kubectl logs <pod-name> -n <namespace> --tail=100`
4. ✅ `kubectl logs <pod-name> -n <namespace> --previous`
5. ✅ `kubectl get events -n <namespace> --sort-by='.lastTimestamp'`
6. ✅ Проверяет service, ingress, configmap, secret, externalsecret
7. ✅ Анализирует и предлагает решение:
   ```
   Проблема: ImagePullBackOff
   Причина: Неправильный registry или отсутствует imagePullSecret

   Решение:
   1. Проверьте image в values.yaml
   2. Добавьте imagePullSecrets
   ```

### Пример 2: Почему приложение возвращает 502?

**Запрос:**
```
Почему приложение возвращает 502 ошибку?
```

**Что проверяет агент:**
1. ✅ Статус подов
2. ✅ Логи приложения
3. ✅ Service и endpoints
4. ✅ Ingress конфигурация
5. ✅ Readiness probes
6. ✅ Предлагает решение

### Пример 3: Показать логи за последний час

**Запрос:**
```
Покажи логи api-service за последний час с фильтром по ERROR
```

**Что делает агент:**
1. ✅ `kubectl logs -n <namespace> -l app.kubernetes.io/name=api-service --since=1h | grep ERROR`
2. ✅ Анализирует и группирует ошибки

### Пример 4: Проверить использование ресурсов

**Запрос:**
```
Покажи использование CPU и памяти для всех подов приложения
```

**Что делает агент:**
1. ✅ `kubectl top pod -n <namespace> -l app.kubernetes.io/name=<app>`
2. ✅ Сравнивает с limits/requests
3. ✅ Предлагает оптимизацию если нужно

---

## 🔄 Откаты и история

### Пример 1: Откатить на предыдущую версию

**Запрос:**
```
Откати api-service на предыдущую версию
```

**Что делает агент:**
1. ✅ Проверяет историю:
   - `kubectl rollout history deployment/api-service -n <namespace>`
   - `helm history api-service -n <namespace>`
2. ✅ Показывает ревизии
3. ⚠️ Запрашивает подтверждение
4. ⚠️ Выполняет: `kubectl rollout undo deployment/api-service -n <namespace>`
5. ✅ Мониторит: `kubectl rollout status` и `kubectl get pods -w`

### Пример 2: Откатить на конкретную ревизию

**Запрос:**
```
Откати на ревизию 3
```

---

## 🚀 Комплексные задачи

### Пример 1: Полная настройка нового микросервиса

**Запрос:**
```
Настрой полный деплой для нового микросервиса:
- 3 реплики
- Memory: 512Mi limit, 256Mi request
- CPU: 500m limit, 250m request
- Ingress: new-service.example.com с TLS
- ExternalSecret для DB_URL и API_KEY из Vault
- HPA: 3-10 реплик при CPU > 75%
- Healthcheck на /health
```

**Что делает агент:**
1. ✅ Создает полный `kuber/values.yaml` со всеми параметрами
2. ✅ Настраивает все компоненты
3. ✅ Проверяет конфигурацию через helmfile diff
4. ⚠️ Применяет изменения
5. ✅ Проверяет все созданные ресурсы

### Пример 2: Миграция между namespace

**Запрос:**
```
Помоги мигрировать api-service из namespace staging в production
```

**Что делает агент:**
1. ✅ Читает текущую конфигурацию из staging
2. ✅ Создает план миграции с изменениями для production
3. ✅ Создает чеклист необходимых действий
4. ✅ Помогает с каждым шагом миграции

---

## 📊 Мониторинг и метрики

### Пример 1: Настроить ServiceMonitor для Prometheus

**Запрос:**
```
Добавь ServiceMonitor для Prometheus scraping на порту 8080 /metrics
```

### Пример 2: Проверить метрики приложения

**Запрос:**
```
Проверь доступность метрик приложения через Prometheus
```

---

## 🔍 Анализ и проверки

### Пример 1: Проверить конфигурацию

**Запрос:**
```
Проверь конфигурацию приложения на best practices
```

**Что проверяет агент:**
1. ✅ Resources (requests/limits)
2. ✅ Probes (liveness/readiness)
3. ✅ Security (не root пользователь, readOnlyRootFilesystem)
4. ✅ HPA настроен
5. ✅ Ingress TLS
6. ✅ Предлагает улучшения

### Пример 2: Показать текущую конфигурацию

**Запрос:**
```
Покажи текущую конфигурацию deployment в кластере
```

**Что делает агент:**
1. ✅ `kubectl get deployment <app> -n <namespace> -o yaml`
2. ✅ `helm get values <release> -n <namespace>`
3. ✅ Выделяет ключевые параметры

### Пример 3: Сравнить с манифестом

**Запрос:**
```
Сравни текущий values.yaml с тем что задеплоено в кластере
```

**Что делает агент:**
1. ✅ `helmfile diff`
2. ✅ Показывает различия
3. ✅ Объясняет почему есть различия

---

## 💡 Полезные советы

### Будьте конкретны
❌ Плохо: "Исправь приложение"
✅ Хорошо: "Почему под api-service-xxx не стартует?"

### CI/CD workflow
Если есть `.gitlab-ci.yaml`, агент предложит:
- Commit и push для автодеплоя через CI
- Или ручной helmfile sync

### Vault пути
Агент автоматически определяет правильные пути в Vault:
`<project>/<namespace>/<app-name>`

### Git проверки
Агент проверит незакоммиченные изменения перед началом работы

---

## 🎓 Обучающие примеры

### Для начинающих

**Запрос:**
```
Объясни как работает deployment приложения через helmfile
```

**Запрос:**
```
Покажи как добавить переменную окружения шаг за шагом
```

### Для продвинутых

**Запрос:**
```
Настрой blue-green deployment для api-service
```

**Запрос:**
```
Настрой canary deployment с 10% трафика на новую версию
```

---

**Версия:** 1.0
**Дата:** 2025-11-21

# DevOps Cluster Charts - Примеры запросов пользователей

Практические примеры работы с субагентом **devops-cluster-charts** для управления инфраструктурными компонентами уровня кластера.

## 📍 Контекст работы

```bash
cd ~/projects/<project>/cluster-charts/<chart-name>/stages/<cluster-name>/
claude
# Субагент devops-cluster-charts активируется автоматически
```

---

## 🔄 Обновление версий чартов

### Пример 1: Обновить cert-manager

**Запрос:**
```
Обнови cert-manager до версии 1.14.0
```

**Что делает агент:**
1. ✅ Читает `helmfile.yaml.gotmpl`
2. ✅ Показывает текущую версию: `1.13.0`
3. ⚠️ Запрашивает подтверждение на изменение
4. ✅ Обновляет версию: `1.13.0 → 1.14.0`
5. ✅ Выполняет `helmfile diff -l name=cert-manager`:
   ```
   Изменения:
   - Image: quay.io/jetstack/cert-manager-controller:v1.13.0 → v1.14.0
   - Image: quay.io/jetstack/cert-manager-webhook:v1.13.0 → v1.14.0
   - Image: quay.io/jetstack/cert-manager-cainjector:v1.13.0 → v1.14.0
   ```
6. ⚠️ Запрашивает подтверждение на `helmfile sync`
7. ✅ Проверяет статус подов:
   ```bash
   kubectl get pods -n cert-manager
   kubectl rollout status deployment/cert-manager -n cert-manager
   ```

### Пример 2: Обновить ingress-nginx

**Запрос:**
```
Обнови ingress-nginx до последней версии
```

**Что делает агент:**
1. ✅ Читает текущую версию
2. ✅ Ищет последнюю стабильную версию (web search если нужно)
3. ✅ Показывает changelog и breaking changes
4. ⚠️ Запрашивает подтверждение
5. ✅ Обновляет и проверяет

### Пример 3: Откатить версию чарта

**Запрос:**
```
Откати cert-manager на предыдущую версию
```

**Что делает агент:**
1. ✅ Проверяет историю: `helm history cert-manager -n cert-manager`
2. ✅ Показывает доступные ревизии
3. ⚠️ Запрашивает подтверждение
4. ⚠️ Выполняет rollback: `helm rollback cert-manager <revision> -n cert-manager`
5. ✅ Мониторит процесс

---

## ⚙️ Изменение параметров

### Пример 1: Увеличить ресурсы для ingress-nginx

**Запрос:**
```
Увеличь memory limit до 1Gi и добавь CPU request 500m для ingress-nginx
```

**Что делает агент:**
1. ✅ Читает текущий `values.yaml`
2. ✅ Показывает текущие значения:
   ```yaml
   resources:
     limits:
       memory: 512Mi
     requests:
       cpu: 250m
   ```
3. ⚠️ Запрашивает подтверждение на изменение
4. ✅ Обновляет:
   ```yaml
   resources:
     limits:
       memory: 1Gi
     requests:
       cpu: 500m
   ```
5. ✅ `helmfile diff -l name=ingress-nginx`
6. ⚠️ Применяет через `helmfile sync`
7. ✅ Проверяет: `kubectl top pod -n ingress-nginx`

### Пример 2: Изменить количество реплик

**Запрос:**
```
Увеличь количество реплик cert-manager до 3
```

### Пример 3: Добавить nodeSelector

**Запрос:**
```
Настрой ingress-nginx controller на работу только на dedicated nodes с label ingress=true
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ⚠️ Запрашивает подтверждение
3. ✅ Добавляет:
   ```yaml
   nodeSelector:
     ingress: "true"
   tolerations:
     - key: dedicated
       operator: Equal
       value: ingress
       effect: NoSchedule
   ```
4. ✅ Применяет и проверяет размещение подов

---

## 🐛 Диагностика и дебаг

### Пример 1: Почему cert-manager не работает?

**Запрос:**
```
Почему cert-manager-webhook не стартует?
```

**Что делает агент (всё без подтверждения):**
1. ✅ `kubectl get pods -n cert-manager`
2. ✅ `kubectl describe pod cert-manager-webhook-xxx -n cert-manager`
3. ✅ `kubectl logs cert-manager-webhook-xxx -n cert-manager`
4. ✅ `kubectl logs cert-manager-webhook-xxx -n cert-manager --previous`
5. ✅ `kubectl get events -n cert-manager --sort-by='.lastTimestamp'`
6. ✅ Проверяет ValidatingWebhookConfiguration:
   ```bash
   kubectl get validatingwebhookconfiguration
   kubectl describe validatingwebhookconfiguration cert-manager-webhook
   ```
7. ✅ Анализирует и предлагает решение:
   ```
   Проблема: Webhook не может подключиться к API server
   Причина: Сертификат webhook истек

   Решение:
   1. Удалить webhook pod для regeneration сертификата
   2. Или пересоздать webhook secret
   ```

### Пример 2: Проблема с сертификатами

**Запрос:**
```
Почему cert-manager не выпускает сертификаты?
```

**Что проверяет агент:**
1. ✅ ClusterIssuer: `kubectl get clusterissuer`
2. ✅ Certificates: `kubectl get certificate --all-namespaces`
3. ✅ CertificateRequests: `kubectl get certificaterequest --all-namespaces`
4. ✅ Challenges: `kubectl get challenge --all-namespaces`
5. ✅ Логи cert-manager controller
6. ✅ Предлагает решение

### Пример 3: Проверка ingress-nginx

**Запрос:**
```
Проверь здоровье ingress-nginx контроллера
```

**Что делает агент:**
1. ✅ Статус подов и deployment
2. ✅ Логи на ошибки
3. ✅ Metrics: connections, requests, errors
4. ✅ ConfigMap конфигурация
5. ✅ Service и endpoints
6. ✅ Отчет о состоянии

---

## 🔧 Конфигурация cert-manager

### Пример 1: Добавить новый ClusterIssuer

**Запрос:**
```
Добавь ClusterIssuer для Let's Encrypt staging
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ⚠️ Запрашивает подтверждение
3. ✅ Добавляет конфигурацию ClusterIssuer:
   ```yaml
   clusterIssuers:
     - name: letsencrypt-staging
       server: https://acme-staging-v02.api.letsencrypt.org/directory
       email: admin@example.com
       privateKeySecretRef:
         name: letsencrypt-staging
       solvers:
         - http01:
             ingress:
               class: nginx
   ```
4. ✅ Применяет и проверяет:
   ```bash
   kubectl get clusterissuer letsencrypt-staging
   kubectl describe clusterissuer letsencrypt-staging
   ```

### Пример 2: Настроить DNS01 challenge

**Запрос:**
```
Настрой cert-manager для использования CloudFlare DNS01 challenge
```

### Пример 3: Проверить статус выпуска сертификатов

**Запрос:**
```
Покажи статус всех сертификатов в кластере
```

**Что делает агент:**
1. ✅ `kubectl get certificate --all-namespaces`
2. ✅ Группирует по статусу (Ready, Not Ready, Expired)
3. ✅ Для проблемных сертификатов показывает детали
4. ✅ Предлагает действия по исправлению

---

## 🌐 Конфигурация ingress-nginx

### Пример 1: Добавить custom annotation

**Запрос:**
```
Добавь в ingress-nginx controller custom headers для безопасности
```

**Что делает агент:**
1. ✅ Читает values.yaml
2. ⚠️ Запрашивает подтверждение
3. ✅ Добавляет:
   ```yaml
   controller:
     config:
       add-headers: "custom-headers"
       hsts: "true"
       hsts-max-age: "31536000"
       ssl-protocols: "TLSv1.2 TLSv1.3"
   ```

### Пример 2: Настроить rate limiting

**Запрос:**
```
Настрой rate limiting для ingress-nginx: 100 req/s per IP
```

### Пример 3: Включить модули

**Запрос:**
```
Включи ModSecurity в ingress-nginx
```

---

## 📊 Мониторинг и метрики

### Пример 1: Проверить метрики cert-manager

**Запрос:**
```
Покажи метрики cert-manager: количество сертификатов, успешность renewal
```

**Что делает агент:**
1. ✅ Проверяет endpoint метрик
2. ✅ `kubectl get --raw /metrics` для cert-manager
3. ✅ Парсит и показывает ключевые метрики
4. ✅ Выявляет проблемы

### Пример 2: Проверить performance ingress-nginx

**Запрос:**
```
Покажи статистику ingress-nginx: RPS, latency, errors
```

### Пример 3: Настроить ServiceMonitor

**Запрос:**
```
Добавь ServiceMonitor для Prometheus scraping cert-manager метрик
```

---

## 🔐 Безопасность

### Пример 1: Проверить SecurityContext

**Запрос:**
```
Проверь настройки безопасности cert-manager pods
```

**Что проверяет агент:**
1. ✅ SecurityContext (runAsNonRoot, readOnlyRootFilesystem)
2. ✅ NetworkPolicies
3. ✅ RBAC permissions
4. ✅ Pod Security Policies/Standards
5. ✅ Предлагает улучшения

### Пример 2: Ограничить NetworkPolicy

**Запрос:**
```
Создай NetworkPolicy для cert-manager разрешающую только необходимые соединения
```

---

## 🏗️ Комплексные задачи

### Пример 1: Полная настройка cert-manager

**Запрос:**
```
Настрой cert-manager с:
- Let's Encrypt prod и staging issuers
- CloudFlare DNS01
- Webhook для проверки
- Prometheus мониторинг
```

**Что делает агент:**
1. ✅ Проверяет текущую конфигурацию
2. ✅ Создает полный values.yaml
3. ✅ Настраивает все компоненты
4. ✅ Применяет через helmfile
5. ✅ Проверяет работоспособность каждого компонента

### Пример 2: Миграция между кластерами

**Запрос:**
```
Помоги мигрировать конфигурацию cert-manager из dev в prod кластер
```

**Что делает агент:**
1. ✅ Читает конфигурацию из dev
2. ✅ Создает адаптированную версию для prod
3. ✅ Создает чеклист миграции
4. ✅ Помогает с каждым шагом

---

## 🔍 Анализ и проверки

### Пример 1: Проверить конфигурацию перед применением

**Запрос:**
```
Проверь что изменения в ingress-nginx безопасны
```

**Что делает агент:**
1. ✅ `helmfile diff`
2. ✅ `helmfile template` для просмотра манифестов
3. ✅ Анализирует изменения
4. ✅ Проверяет на breaking changes
5. ✅ Оценивает impact на существующие ingress

### Пример 2: Сравнить с production best practices

**Запрос:**
```
Сравни конфигурацию cert-manager с production best practices
```

**Что проверяет:**
1. ✅ High Availability (replicas >= 2)
2. ✅ Resource limits установлены
3. ✅ PodDisruptionBudget настроен
4. ✅ SecurityContext правильный
5. ✅ Monitoring настроен
6. ✅ Предлагает улучшения

### Пример 3: Показать различия между default и текущим

**Запрос:**
```
Покажи что изменено в values.yaml по сравнению с ../default/values.yaml
```

**Что делает агент:**
1. ⚠️ Запрашивает доступ к `../default/values.yaml`
2. ✅ Читает оба файла
3. ✅ Показывает diff
4. ✅ Объясняет причины различий

---

## 🚨 Аварийные ситуации

### Пример 1: Срочно откатить изменения

**Запрос:**
```
СРОЧНО! Cert-manager сломан после обновления, откати назад
```

**Что делает агент:**
1. ✅ Проверяет текущее состояние
2. ✅ `helm history cert-manager -n cert-manager`
3. ⚠️ Запрашивает подтверждение (минимальное)
4. ⚠️ Быстрый rollback: `helm rollback cert-manager -n cert-manager`
5. ✅ Мониторит восстановление
6. ✅ Проверяет что сертификаты работают

### Пример 2: Ingress-nginx не отвечает

**Запрос:**
```
Ingress-nginx controller не отвечает, весь трафик недоступен!
```

**Что делает агент:**
1. ✅ Быстрая диагностика:
   - Статус подов
   - Логи последних минут
   - Service и endpoints
2. ✅ Предлагает immediate actions:
   - Restart подов
   - Scale up replicas
   - Проверить node connectivity
3. ⚠️ Выполняет после подтверждения

---

## 📚 Обучающие примеры

### Для начинающих

**Запрос:**
```
Объясни как работает cert-manager step by step
```

**Запрос:**
```
Покажи как создается сертификат от Let's Encrypt
```

### Для продвинутых

**Запрос:**
```
Настрой multi-cluster cert-manager с shared CRDs
```

**Запрос:**
```
Оптимизируй ingress-nginx для высоких нагрузок (100k+ RPS)
```

---

## 💡 Полезные советы

### Проверка default values
Всегда проверяйте `../default/values.yaml` для понимания базовых настроек

### Версионность
При обновлениях читайте changelog и breaking changes

### Влияние на кластер
Помните: изменения влияют на **весь кластер**, будьте внимательны

### Тестирование
Сначала тестируйте на dev/staging, затем prod

---

**Версия:** 1.0
**Дата:** 2025-11-21
